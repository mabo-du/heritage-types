node_modules/
__pycache__/
*.pyc
dist/
tsp-output/
.DS_Store
python/heritage_models/*.egg-info
python/heritage_vocab/*.egg-info

# TypeSpec emitter treats bundleId as a relative URL → leaks subdirs.
https:/

# Cache & Carry's vocabulary DB when cached locally for dev.
*.vocab_cache.db

# Local dev artifacts — GitNexus index and Claude skill configs.
.claude/
.ctx/
typescript/package-lock.json

# Charter / agent session artifacts
.charter/
*.charter-session
.claude/local/
.cursor/cache/
.hk/
.env
